{
  "name": "gulp-mu-css-ps",
  "version": "0.1.0",
  "private": true,
  "description": "Root project for the microCSS and microPS node modules.",
  "type": "module",
  "scripts": {
    "test": "gulp test",
    "test:microps": "gulp test:microps",
    "test:legacy-migration": "gulp test:legacy-migration",
    "examples:render": "gulp examples:render",
    "build:publish": "gulp build:publish",
    "demos:build": "node tools/build-demos.mjs"
  },
  "devDependencies": {
    "gulp": "^5.0.0",
    "gulp-cli": "^3.1.0"
  }
}
